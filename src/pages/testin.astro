---
import Layout from "../layouts/Layout.astro";
import {ArrowButton} from "../components/arrowbutton/ArrowButton";
---

<Layout title="Welcome to Astro.">
    <br>
  <ArrowButton client:only="react" onClick={() => {}}/>
</Layout>
